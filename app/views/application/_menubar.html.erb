<nav class="menuBar">
  <div>
    <%= link_to "Главная", root_url %>

    <%# if user_signed_in? && current_user.admin? %>

   	<%= link_to 'Эффекты', effects_path %>
    <%= link_to 'Обсуждения', questions_path %>
    <%= link_to 'Избранное', favorites_path %>
    <%= link_to 'Коллекции', collections_path %>
    </ul>
  </div>

  <div>
    <% if user_signed_in? %>
      <button><%= link_to current_user.email, edit_user_registration_path %></button>
      <button><%= link_to "Выйти", destroy_user_session_path, data: { "turbo-method": :delete } %></button>
    <% else %>
      <button><%= link_to "Войти", new_user_session_path %></button>
      <button><%= link_to "Зарегистрироваться", new_user_registration_path %></button>
    <% end %>
  </div>
</nav>