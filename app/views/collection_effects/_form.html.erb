<%= form_with(model: collection_effect) do |form| %>
  <% if collection_effect.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(collection_effect.errors.count, "error") %> prohibited this collection_effect from being saved:</h2>

      <ul>
        <% collection_effect.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :collection_id, style: "display: block" %>
    <%= form.text_field :collection_id %>
  </div>

  <div>
    <%= form.label :effect_id, style: "display: block" %>
    <%= form.text_field :effect_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
